# JSON parser for HTTP body
[PARSER]
    Name        json_parser
    Format      json

# CSV parser for UDP
[PARSER]
    Name        csv_parser
    Format      regex
    Regex       ^(?<timestamp>[^,]+),(?<counter>[^,]+),(?<message>.+)$

